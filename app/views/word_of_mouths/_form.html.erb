<%= form_with(model: word_of_mouth, local: true) do |form| %>
  <% if word_of_mouth.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(word_of_mouth.errors.count, "error") %> prohibited this word_of_mouth from being saved:</h2>

      <ul>
        <% word_of_mouth.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
